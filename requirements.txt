# Socrates Instagram Automation — Core Dependencies
# Install: pip install -r requirements.txt

# HTTP requests
httpx>=0.27.0
requests>=2.31.0

# Image processing
Pillow>=10.0.0

# Excel / data
openpyxl>=3.1.0

# Environment config
python-dotenv>=1.0.0

# AI Creative Studio — reasoning agents (structured outputs, prompt caching)
anthropic>=0.69.0

# Cloud image upload (optional — used by image_generator)
cloudinary>=1.36.0

# Dev / test tools
pytest>=8.0.0
ruff>=0.4.0

# ── Optional: Better beat detection ──────────────────────────────────────────
# Uncomment to install librosa for more accurate musical beat detection.
# Without it, the pipeline falls back to ffmpeg ebur128 (works fine).
# librosa>=0.10.0  # + numpy, scipy, soundfile (heavy, ~150MB)

# ── Web API (optional — for receipt upload endpoint) ──────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.12
supabase>=2.5.0
pydantic>=2.0.0

# ── Live team pipeline dashboard ──────────────────────────────────────────────
flask>=3.0.0

# ── Voiceover generation ───────────────────────────────────────────────────────
edge-tts>=6.1.0

pytrends>=4.9.2  # Google Trends (optional; Trend Scout degrades to GNews if unavailable)
