[pytest]
# A test that `return`s a value (instead of asserting) is almost always a bug —
# historically several integration tests did `return True/False` and swallowed
# AssertionError, so they passed unconditionally. Make that a hard error.
filterwarnings =
    error::pytest.PytestReturnNotNoneWarning
